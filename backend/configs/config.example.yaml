app:
  name: mini-store-go-api
  env: development
  host: 0.0.0.0
  port: 8080
  read_timeout: 10s
  write_timeout: 10s
  idle_timeout: 60s
  shutdown_timeout: 10s

auth:
  access_secret: change-me-access-secret
  refresh_secret: change-me-refresh-secret
  access_ttl: 15m
  refresh_ttl: 720h
  access_cookie_name: mini_store_access_token
  refresh_cookie_name: mini_store_refresh_token
  session_cart_cookie_name: session_cart_id
  cookie_domain: ""
  cookie_secure: false
  cookie_http_only: true
  cookie_same_site: lax

database:
  dsn: postgres://postgres:postgres@localhost:5432/mini_store?sslmode=disable
  auto_migrate: false
  max_idle_conns: 10
  max_open_conns: 50
  conn_max_lifetime: 30m

redis:
  addr: localhost:6379
  password: ""
  db: 0
  pool_size: 10

log:
  level: debug
  encoding: console
  development: true

cors:
  allowed_origins:
    - http://localhost:5173
    - http://localhost:3000
  allowed_methods:
    - GET
    - POST
    - PUT
    - PATCH
    - DELETE
    - OPTIONS
  allowed_headers:
    - Origin
    - Content-Type
    - Accept
    - Authorization
    - X-Requested-With
    - X-Request-Id
  exposed_headers:
    - X-Request-Id
  allow_credentials: true
  max_age: 12h
